<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bhupendra.production123">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher311"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher311_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".MainActivity"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize|adjustPan"/>
        <activity android:name=".AudioActivity"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize|adjustPan"></activity>
        <activity android:name=".SecActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserListActivity">

        </activity>
    </application>

</manifest>